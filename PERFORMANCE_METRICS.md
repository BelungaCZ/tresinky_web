🏠 [Главная](README.md) | 📋 [Changelog](CHANGELOG.md) | 🚀 [Environment Setup](docs/environment_setup.md) | 🌐 [Deployment Guide](docs/deployment_guide.md) | 📊 **Performance Metrics** | 📱 [Mobile Testing Guide](MOBILE_TESTING_GUIDE.md) | ⚡ [Optimization Plan](OPTIMIZATION_PLAN.md)

---

# Метрики производительности - до и после оптимизации

## Связанная документация

- **[Mobile Testing Guide](MOBILE_TESTING_GUIDE.md)** - Детальные инструкции по тестированию производительности
- **[Optimization Plan](OPTIMIZATION_PLAN.md)** - Комплексный план оптимизации сайта
- **[Implementation Plan](IMPLEMENTATION_PLAN.md)** - Общий план реализации изменений

## Базовые метрики (до оптимизации)

### Главная страница (/)
| Метрика | Desktop | Mobile | Примечания |
|---------|---------|--------|------------|
| Performance Score | ~75-80 | ~65-70 | Baseline |
| LCP | 2.8-3.2s | 3.5-4.0s | Из-за предзагруженных webp |
| FID | 50-80ms | 100-150ms | Baseline |
| CLS | 0.05-0.1 | 0.1-0.15 | Baseline |
| Initial Payload | ~2.5MB | ~2.5MB | Включая webp preloads |

### Страница "Sad" (/sad)
| Метрика | Desktop | Mobile | Примечания |
|---------|---------|--------|------------|
| Performance Score | ~70-75 | ~60-65 | Много изображений |
| LCP | 3.0-3.5s | 4.0-4.5s | Большие webp без lazy |
| FID | 60-90ms | 120-180ms | Baseline |
| CLS | 0.08-0.12 | 0.12-0.18 | Layout shifts |
| Image Transfer | ~6MB | ~6MB | Full размер на всех устройствах |

### Страница "О нас" (/about)
| Метрика | Desktop | Mobile | Примечания |
|---------|---------|--------|------------|
| Performance Score | ~70-75 | ~60-65 | Baseline |
| LCP | 2.5-3.0s | 3.5-4.0s | Baseline |
| FID | 40-70ms | 100-140ms | Baseline |
| CLS | 0.06-0.1 | 0.1-0.15 | Baseline |

## Целевые метрики (после оптимизации)

### Главная страница (/)
| Метрика | Desktop Target | Mobile Target | Улучшение |
|---------|---------------|--------------|-----------|
| Performance Score | 95+ | 90+ | +20-25 points |
| LCP | <2.0s | <2.5s | 30-40% быстрее |
| FID | <50ms | <100ms | Сохранить уровень |
| CLS | <0.1 | <0.1 | Стабильность |
| Initial Payload | <1.5MB | <1.5MB | 40% меньше |

### Страница "Sad" (/sad)
| Метрика | Desktop Target | Mobile Target | Улучшение |
|---------|---------------|--------------|-----------|
| Performance Score | 90+ | 85+ | +20 points |
| LCP | <2.5s | <2.5s | 25-40% быстрее |
| FID | <50ms | <100ms | Стабильность |
| CLS | <0.1 | <0.1 | Улучшение стабильности |
| Image Transfer | ~4MB | ~2MB | 50% меньше на mobile |

### Страница "О нас" (/about)
| Метрика | Desktop Target | Mobile Target | Улучшение |
|---------|---------------|--------------|-----------|
| Performance Score | 90+ | 85+ | +20 points |
| LCP | <2.5s | <2.5s | Стабильность/улучшение |
| FID | <50ms | <100ms | Стабильность |
| CLS | <0.1 | <0.1 | Стабильность |

## Инструменты для измерения

### 1. Lighthouse CI (автоматизированный)
```bash
# Установка
npm install -g @lhci/cli

# Запуск тестов
lhci autorun --upload.target=temporary-public-storage
```

### 2. PageSpeed Insights API
```bash
# Пример запроса
curl "https://www.googleapis.com/pagespeed/v5/runPagespeed?url=https://sad-tresinky-cetechovice.cz&category=performance&strategy=mobile"
```

### 3. Web Vitals JavaScript
```javascript
// Проверка метрик в консоли браузера
WebVitals.getMetrics(); 

// Подписка на события
WebVitals.onMetric(function(metric) {
    console.log(metric.name + ':', metric.value, metric.rating);
});
```

## Как измерять производительность после изменений

### ОБЯЗАТЕЛЬНАЯ ПРОЦЕДУРА ТЕСТИРОВАНИЯ

**После каждого изменения кода, влияющего на производительность, следуйте этому протоколу:**

#### Шаг 1: Подготовка к измерению
1. **Закрыть все лишние вкладки браузера**
2. **Очистить кэш** (Ctrl+Shift+Delete или F12 → Application → Storage → Clear site data)
3. **Отключить расширения браузера** (режим инкогнито или отключить в настройках)
4. **Подключение:** Использовать стабильное интернет-соединение

#### Шаг 2: Baseline измерения (ДО изменений)
```bash
# 1. Chrome DevTools Lighthouse
# Открыть DevTools (F12) → Lighthouse
# Выбрать: Performance, Mobile, Clear storage, Simulated throttling
# Запустить "Generate report" 3 раза, взять среднее значение

# 2. PageSpeed Insights
# Перейти на https://pagespeed.web.dev/
# Ввести URL сайта, дождаться завершения
# Сохранить скриншот или записать значения

# 3. Network Analysis  
# DevTools → Network → Disable cache
# Перезагрузить страницу (Ctrl+F5)
# Записать: DOMContentLoaded, Load time, Transfer size
```

#### Шаг 3: Внесение изменений
- Внести планируемые изменения в код
- Задеплоить изменения (локально или на сервер)
- Убедиться, что изменения применились

#### Шаг 4: Измерения ПОСЛЕ изменений
```bash
# Повторить все тесты из Шага 2
# Важно: Использовать те же условия (браузер, соединение, etc.)
```

#### Шаг 5: Анализ и документирование
```markdown
## Результаты измерений [ДАТА]

### Изменения в коде:
- [Описание внесенных изменений]

### Lighthouse Scores:
| Метрика | До | После | Изменение |
|---------|----|----|-----------|
| Performance | 75 | 85 | +10 ✅ |
| LCP | 3.2s | 2.8s | -0.4s ✅ |
| FID | 120ms | 80ms | -40ms ✅ |
| CLS | 0.15 | 0.08 | -0.07 ✅ |

### Network Analysis:
| Метрика | До | После | Изменение |
|---------|----|----|-----------|
| Transfer Size | 2.5MB | 1.8MB | -28% ✅ |
| DOMContentLoaded | 1.2s | 0.9s | -0.3s ✅ |
| Load Time | 4.1s | 3.2s | -0.9s ✅ |

### Выводы:
- [Анализ результатов]
- [Рекомендации для дальнейших улучшений]
```

### Инструменты для автоматизации

#### 1. Bash скрипт для быстрых проверок
```bash
#!/bin/bash
# performance-check.sh

echo "🚀 Запуск проверки производительности..."

# PageSpeed Insights API
echo "📊 PageSpeed Insights Mobile:"
curl -s "https://www.googleapis.com/pagespeed/v5/runPagespeed?url=https://sad-tresinky-cetechovice.cz&strategy=mobile" | \
jq '.lighthouseResult.categories.performance.score * 100'

echo "📊 PageSpeed Insights Desktop:"
curl -s "https://www.googleapis.com/pagespeed/v5/runPagespeed?url=https://sad-tresinky-cetechovice.cz&strategy=desktop" | \
jq '.lighthouseResult.categories.performance.score * 100'

echo "✅ Проверка завершена"
```

#### 2. Python скрипт для мониторинга
```python
import requests
import json
from datetime import datetime

def check_pagespeed(url, strategy='mobile'):
    api_url = f"https://www.googleapis.com/pagespeed/v5/runPagespeed"
    params = {
        'url': url,
        'strategy': strategy,
        'category': 'performance'
    }
    
    response = requests.get(api_url, params=params)
    data = response.json()
    
    return {
        'score': data['lighthouseResult']['categories']['performance']['score'] * 100,
        'lcp': data['lighthouseResult']['audits']['largest-contentful-paint']['numericValue'],
        'fid': data['lighthouseResult']['audits']['max-potential-fid']['numericValue'],
        'cls': data['lighthouseResult']['audits']['cumulative-layout-shift']['numericValue']
    }

# Использование
url = 'https://sad-tresinky-cetechovice.cz'
mobile_results = check_pagespeed(url, 'mobile')
desktop_results = check_pagespeed(url, 'desktop')

print(f"Mobile Score: {mobile_results['score']}")
print(f"Desktop Score: {desktop_results['score']}")
```

## Мониторинг в реальном времени

### Настройка Google Analytics 4
```javascript
// Отправка Web Vitals в GA4
WebVitals.onMetric(function(metric) {
    gtag('event', metric.name, {
        event_category: 'Web Vitals',
        value: Math.round(metric.value),
        metric_rating: metric.rating,
        non_interaction: true
    });
});
```

### Настройка собственного endpoint
**ПРИМЕЧАНИЕ: Endpoint отключен для устранения ошибок 404. Метрики собираются локально.**

В `app.py` добавить маршрут для сбора метрик (опционально):
```python
@app.route('/api/web-vitals', methods=['POST'])
def collect_web_vitals():
    data = request.get_json()
    # Сохранить метрики в базу данных
    logger.info(f"Web Vitals: {data}")
    return {'status': 'ok'}
```

### Локальный анализ Web Vitals
Для анализа метрик производительности в браузере (только в development режиме):
```javascript
// В консоли браузера (F12 → Console) - только в development
console.log('Web Vitals Metrics:', WebVitals.getMetrics());

// Подписка на новые метрики
WebVitals.onMetric(function(metric) {
    console.log('New Web Vital:', metric);
});
```

### Включение отправки на сервер
**ПРИМЕЧАНИЕ: Web Vitals полностью отключен в production для оптимизации производительности.**

Чтобы включить отправку метрик на сервер в development, измените в `static/js/web-vitals.js`:
```javascript
const CONFIG = {
    endpoint: '/api/web-vitals',
    debug: true, // Включить логирование
    sendToServer: true // Включить отправку на сервер
};
```

## Чек-лист верификации улучшений

### ✅ Удаление предзагрузки
- [ ] Убедиться, что на главной странице нет запросов к history.webp и present.webp
- [ ] Проверить размер initial payload уменьшился на 30-40%
- [ ] Верифицировать LCP улучшился на главной странице

### ✅ Lazy Loading
- [ ] Проверить, что webp изображения загружаются только при скролле
- [ ] Убедиться, что логотип загружается сразу (eager)
- [ ] Верифицировать Network tab показывает отложенную загрузку

### ✅ Responsive Images
- [ ] На мобильных устройствах загружаются thumbnail (400w)
- [ ] На desktop загружаются full размер (800w)
- [ ] Трафик на мобильных уменьшился на 40-50%

### ✅ Critical CSS
- [ ] First Paint происходит быстрее
- [ ] Нет FOUC (Flash of Unstyled Content)
- [ ] Critical styles отрендерены до загрузки style.css

### ✅ Web Vitals Tracking
- [ ] Скрипт web-vitals.js загружается и работает только в development режиме
- [ ] В production режиме web-vitals.js полностью отключен для оптимизации производительности
- [ ] Метрики собираются локально только в development (endpoint отключен для устранения 404 ошибок)
- [ ] WebVitals.getMetrics() возвращает данные в консоли только в development
- [ ] Нет ошибок 404 в Network tab для /api/web-vitals
- [ ] В production нет лишних HTTP запросов для web-vitals

## Отчетность

### Еженедельный мониторинг
1. Запуск Lighthouse тестов для ключевых страниц
2. Проверка PageSpeed Insights scores
3. Анализ Web Vitals данных из GA4/собственной системы
4. Документирование изменений в производительности

### Ключевые KPI
- **Performance Score:** Mobile >85, Desktop >90
- **Core Web Vitals:** Все в "Good" зоне
- **Page Load Time:** <2.5s на 3G соединении
- **Image Transfer Reduction:** 40%+ на мобильных

## Базовая линия для сравнения

### Текущее состояние (3 июня 2025)
**До оптимизации:**
- Предзагрузка webp на всех страницах: ✅ Удалена
- Lazy loading для галереи: ✅ Добавлен везде  
- Responsive images: ✅ Реализованы с srcset
- Critical CSS inline: ✅ Добавлены в base.html
- Web Vitals tracking: ✅ Настроен

**Следующие измерения должны показать значительные улучшения в:**
- Initial payload размере
- LCP времени на всех страницах
- Overall performance scores
- Mobile user experience

---

## 🔗 См. также

- **[Mobile Testing Guide](MOBILE_TESTING_GUIDE.md)** - Пошаговые инструкции по тестированию
- **[Optimization Plan](OPTIMIZATION_PLAN.md)** - Техническая реализация оптимизаций
- **[README](README.md)** - Общая документация проекта 